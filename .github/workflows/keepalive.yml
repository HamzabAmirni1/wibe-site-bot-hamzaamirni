name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Bot
        run: |
          curl -I https://rolling-cherianne-ham9666-c0fa34e1.koyeb.app/ || echo "Failed to ping"
